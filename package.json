{
  "name": "keepalive-monorepo",
  "private": true,
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "postinstall": "node scripts/patch-vercel-sandbox-sourcemaps.mjs",
    "lint": "pnpm --filter dashboard lint",
    "fmt": "pnpm --filter dashboard fmt",
    "test": "pnpm --filter dashboard test"
  },
  "pnpm": {
    "overrides": {
      "kysely": "0.28.17"
    }
  }
}
